# 哈特曼传感器项目 Notes

## 1. 项目背景

当前项目是一个哈特曼 / Shack-Hartmann 波前传感器相关实验系统。  
目前光路结构已经基本确定，用户已经了解哈特曼传感器的基本工作原理。当前重点问题包括：

- 哈特曼点阵初始化参考位置如何获得；
- 光阑大小对点阵范围和测量结果的影响；
- DM 可变形镜安装前后，系统有效孔径如何变化；
- 传感器尺寸、DM 尺寸与点阵覆盖范围如何匹配；
- 后续程序应该如何组织和实现。

---

## 2. 当前光路结构

根据讨论中的光路图，系统大致为：

```text
561 激光
→ 光阑
→ L1 / L2 组成的 4f 系统
→ 平面镜
→ DM 可变形镜位置
→ L3 / L4 组成的 4f 系统
→ 哈特曼传感器
```

当前阶段还没有实际安装 DM 可变形镜，DM 位置暂时由一块尺寸较大的平面镜代替。

因此当前光路与未来真实 DM 光路之间存在一个重要差别：

```text
当前：大平面镜不会明显裁剪光束
未来：真实 DM 的有效口径可能会限制光束
```

---

## 3. 哈特曼传感器的基本测量逻辑

哈特曼传感器不是直接测量相位，而是通过微透镜阵列形成的光斑位移来测量局部波前斜率。

基本流程为：

```text
参考状态图像
→ 提取参考光斑中心

加载相位后的图像
→ 提取当前光斑中心

当前光斑中心 - 参考光斑中心
→ 光斑位移 dx, dy

光斑位移 / 微透镜焦距
→ 局部波前斜率

局部波前斜率
→ 波前重建
→ 相位分布
```

更具体地说：

```text
dx = x_measure - x_reference
dy = y_measure - y_reference
```

如果相机像元尺寸为 `p`，微透镜焦距为 `f_MLA`，则：

```text
slope_x ≈ dx_pixel × p / f_MLA
slope_y ≈ dy_pixel × p / f_MLA
```

波前高度 `W(x, y)` 与相位 `φ(x, y)` 的关系为：

```text
φ(x, y) = 2π W(x, y) / λ
```

其中 `λ` 为激光波长。

---

## 4. 为什么需要参考图像，而不是直接使用理想点阵

实验系统中，真实参考点阵通常不是完美规则网格。原因包括：

- 相机与微透镜阵列安装存在倾斜；
- 光轴可能有偏移；
- 微透镜阵列本身存在制造误差；
- 4f 系统存在像差或畸变；
- 入射光不一定是完美平面波；
- 光路调试误差会导致点阵整体旋转、缩放或偏移。

因此程序不应直接使用理论理想点阵作为基准，而应使用实际采集到的参考图像。

推荐逻辑：

```text
DM 加载 flat / zero / reference 状态
→ 拍摄 reference 图像
→ 提取 reference spots

DM 加载目标相位
→ 拍摄 measurement 图像
→ 提取 measurement spots

measurement spots - reference spots
→ 光斑位移
```

这样得到的是目标相位相对于参考状态造成的波前变化。

---

## 5. 初始化参考点阵的建议

由于光路调试原因，哈特曼传感器无法自动获得初始化点位置，因此应先拍摄一张无相位添加的参考图像。

推荐初始化流程：

```text
1. 相机采集参考图像
2. 背景扣除
3. 图像滤波 / 平滑
4. 检测所有光斑
5. 计算每个光斑中心
6. 给光斑编号
7. 保存 reference_points
```

示例数据结构：

```text
reference_points = [
    {id: 1, x0: ..., y0: ...},
    {id: 2, x0: ..., y0: ...},
    ...
]
```

后续所有测量都基于该参考点表。

---

## 6. 光阑大小对系统的影响

### 6.1 光阑较小时

光阑会限制进入系统的有效孔径：

```text
光阑变小
→ 入射光束变窄
→ 照亮的微透镜区域变小
→ 哈特曼点阵范围变小
→ 有效光斑数量减少
```

这种情况下只能测量光束中心区域的波前，边缘像差信息无法获得。

### 6.2 光阑变大时

光阑变大后，进入系统的光束范围变大：

```text
光阑变大
→ 照亮区域变大
→ 点阵范围变大
→ 有效光斑数量增加
```

但光阑过大也可能带来问题：

- 背景光增强；
- 杂散光增加；
- 光斑过曝；
- 边缘光斑变形；
- 光斑重叠；
- 质心计算误差变大。

### 6.3 光阑的合理设置

不要追求点阵刚好完全铺满甚至超过传感器，而应满足：

```text
1. 点阵覆盖传感器大部分区域；
2. 边缘光斑完整；
3. 边缘留有安全余量；
4. 光斑之间不重叠；
5. 光斑不过曝；
6. 背景杂散光较低；
7. 加载最大相位后光斑仍不会跑出传感器。
```

一个合理经验是：

```text
点阵覆盖传感器有效区域的 70% ~ 90% 通常比较安全。
```

---

## 7. 点阵超出传感器范围的影响

如果点阵范围超过传感器，会出现三类主要问题：

```text
1. 光斑丢失
2. 光斑截断
3. 光斑误匹配
```

### 7.1 光斑丢失

如果边缘光斑完全落在传感器外，则对应子孔径没有数据：

```text
无法计算 dx, dy
无法计算局部波前斜率
无法参与相位重建
```

### 7.2 光斑截断

如果光斑只被拍到一部分，质心会偏向图像内部，导致错误的光斑中心。  
这种情况比完全丢失更危险，因为程序可能误以为检测成功，但计算结果错误。

### 7.3 光斑误匹配

如果边缘点消失，程序可能把相邻光斑误认为该点对应光斑，导致错误的局部斜率。

### 7.4 边界余量建议

参考图中最外圈光斑距离图像边界应满足：

```text
边界余量 > 最大可能位移 + 光斑半径 + 安全余量
```

例如：

```text
最大位移：20 px
光斑半径：5 px
安全余量：10 px

边缘光斑中心距离边界最好 > 35 px
```

---

## 8. 当前使用大平面镜代替 DM 时的判断

当前 DM 位置使用的是尺寸较大的平面镜，因此它不会像真实 DM 一样限制有效孔径。

所以当前阶段：

```text
光阑变大
→ 进入后级光路的光束范围变大
→ 哈特曼点阵范围可能继续变大
```

这说明当前光阑仍然可能是限制有效孔径的关键因素。

因此，之前的判断：

```text
光覆盖满 DM 后，超出 DM 范围的光不被反射，所以哈特曼处光尺寸不变
```

在真实 DM 安装后可能成立，但在当前使用大平面镜替代 DM 的条件下不成立。

---

## 9. 换上真实 DM 后的有效孔径判断

如果未来换上真实 DM，则系统有效孔径可能由 DM 决定。

分两种情况：

### 9.1 光阑小于 DM 有效口径

```text
光阑限制光束
→ 光阑大小影响哈特曼点阵范围
```

### 9.2 光阑大于 DM 有效口径

```text
DM 限制光束
→ 继续增大光阑不会明显增加哈特曼点阵范围
```

因此真实 DM 安装后，若光束已经覆盖 DM 有效区域，哈特曼点阵尺寸主要由以下因素决定：

```text
DM 有效口径
L3 / L4 这个 4f 系统的放大倍率
哈特曼微透镜阵列尺寸
相机传感器尺寸
```

理想状态是：

```text
DM 有效区域被完整照亮
哈特曼点阵完整落入传感器
边缘光斑完整且有余量
背景杂散光较低
```

---

## 10. 建议在当前平面镜阶段模拟 DM 有效口径

由于当前没有真实 DM，建议在平面镜附近使用孔径片、光阑或遮挡结构，模拟未来 DM 的有效口径。

目的：

```text
提前验证未来 DM 有效口径经过 L3 / L4 后在哈特曼传感器上的点阵尺寸
```

这样可以避免当前使用大平面镜得到的点阵范围与未来安装 DM 后不一致。

---

## 11. 相机传感器尺寸计算

已知相机参数：

```text
分辨率：1440 × 1080
像元尺寸：3.45 μm
```

有效成像尺寸：

```text
宽度 = 1440 × 3.45 μm = 4968 μm = 4.968 mm
高度 = 1080 × 3.45 μm = 3726 μm = 3.726 mm
```

因此：

```text
传感器有效尺寸 ≈ 4.97 mm × 3.73 mm
对角线 ≈ 6.21 mm
```

图中标称 `1/2.9"` 是传感器光学规格格式，不等于实际物理尺寸。实际有效尺寸应按像素数量乘以像元尺寸计算。

---

## 12. DM 参数与尺寸估算

给定 DM 参数：

```yaml
dm:
    enabled: true
    Nact: 12
    DMmesh: 312
    DMstride: 26
    DMgain: 15.372e-9
    wavelength: 680.0e-9
    mirror_factor: 2.0
    pitch: 400.0e-6
    sigma: 1.125
    width: 5
```

关键参数：

```text
Nact = 12
pitch = 400 μm = 0.4 mm
```

### 12.1 最外侧执行器中心跨度

12 个执行器之间有 11 个间隔：

```text
D = (Nact - 1) × pitch
D = 11 × 0.4 mm
D = 4.4 mm
```

所以最外侧执行器中心跨度为：

```text
4.4 mm × 4.4 mm
```

### 12.2 程序网格 / 近似有效口径

如果按 `Nact × pitch` 估算：

```text
D = 12 × 0.4 mm = 4.8 mm
```

结合：

```text
DMmesh = 312
DMstride = 26
312 / 26 = 12
```

可以理解为程序网格中 DM 宽度对应 12 个 actuator stride。

因此仿真 / 程序中的近似有效口径可取：

```text
DM 有效口径 ≈ 4.8 mm × 4.8 mm
```

### 12.3 使用建议

- 分析 actuator 中心位置时，可用 `4.4 mm`；
- 估算光束覆盖、哈特曼点阵映射尺寸时，可优先使用 `4.8 mm`；
- 如果真实 DM 厂商给出了有效光学口径，应优先使用厂家参数。

---

## 13. DM 反射相位关系

如果 DM 是反射式器件，表面高度变化 `h(x, y)` 会造成两倍光程变化：

```text
OPD ≈ 2h(x, y)
```

对应相位为：

```text
φ_DM ≈ 4πh(x, y) / λ
```

因此需要区分：

```text
1. DM 表面形变量 h(x, y)
2. 反射后的波前高度 W(x, y)
3. 光程差 OPD(x, y)
4. 相位 φ(x, y)
```

这几个量相关，但不能混淆。

---

## 14. 程序设计建议

### 14.1 第一阶段目标

第一版程序不应急于完成完整相位重建，而应先实现稳定的光斑位移测量。

第一阶段目标：

```text
1. 拍参考图；
2. 自动检测参考光斑；
3. 拍测试图；
4. 在参考点附近搜索对应光斑；
5. 计算 dx, dy；
6. 画出位移矢量图。
```

只要光斑跟踪稳定，后面的斜率计算和波前重建就更容易实现。

---

## 15. 推荐程序架构

```text
HartmannSensorSystem
│
├── CameraModule
│   ├── open_camera()
│   ├── capture_frame()
│   └── set_exposure()
│
├── ReferenceModule
│   ├── capture_reference()
│   ├── detect_reference_spots()
│   ├── assign_spot_ids()
│   └── save_reference()
│
├── SpotDetectionModule
│   ├── preprocess_image()
│   ├── detect_spots()
│   ├── calculate_centroid()
│   └── calculate_gaussian_fit_center()
│
├── TrackingModule
│   ├── load_reference_points()
│   ├── search_spots_near_reference()
│   ├── match_spots()
│   └── reject_bad_points()
│
├── SlopeCalculationModule
│   ├── pixel_to_physical()
│   ├── calculate_dx_dy()
│   └── calculate_wavefront_slopes()
│
├── ReconstructionModule
│   ├── reconstruct_by_integration()
│   ├── reconstruct_by_zernike()
│   └── remove_tip_tilt_defocus()
│
└── VisualizationModule
    ├── show_spot_image()
    ├── show_displacement_vectors()
    ├── show_slope_map()
    └── show_reconstructed_wavefront()
```

---

## 16. 主流程建议

### 16.1 参考初始化流程

```text
启动程序
│
├── 是否已有参考点文件？
│
├── 有：
│   ├── 读取参考点
│   ├── 显示在当前图像上
│   └── 用户确认是否可用
│
└── 无：
    ├── 拍摄参考图
    ├── 自动检测参考点
    ├── 用户检查
    └── 保存参考点
```

### 16.2 测量流程

```text
测量循环
│
├── 加载 DM 目标相位
├── 等待系统稳定
├── 拍摄哈特曼测试图像
├── 对每个参考点搜索对应光斑
├── 计算 dx, dy
├── 剔除异常点
├── 计算 slope_x, slope_y
├── 重建波前
└── 显示 / 保存结果
```

---

## 17. 光斑匹配策略

不建议在整幅图里盲目重新找点再匹配，而应基于参考点局部搜索。

推荐策略：

```text
对每个 reference spot：
    在其附近设置搜索窗口
    搜索当前图像中的对应光斑
    计算当前光斑质心
    得到 dx, dy
```

这样可以减少误匹配。

需要设置：

```text
最大允许位移
搜索窗口半径
光斑质量阈值
边缘剔除条件
```

---

## 18. 光斑质量判断

每个光斑都需要质量评估。不合格的点应剔除。

建议检查：

```text
峰值强度是否足够
是否过曝
总能量是否足够
光斑半径是否合理
质心是否在搜索窗口中心附近
光斑是否接触图像边界
拟合残差是否过大
位移是否超过合理范围
```

坏点应标记为：

```text
invalid spot
lost spot
bad subaperture
```

---

## 19. 波前重建方法

### 19.1 区域积分法

根据局部斜率直接积分得到波前。

优点：

```text
直观，容易理解
```

缺点：

```text
对噪声敏感
边界处理复杂
```

### 19.2 Zernike 多项式拟合法

将波前表示为 Zernike 多项式组合：

```text
W(x, y) = a1 Z1(x, y) + a2 Z2(x, y) + a3 Z3(x, y) + ...
```

通过测得的 `∂W/∂x` 和 `∂W/∂y` 反推系数。

优点：

```text
物理意义清晰
可直接分析倾斜、离焦、像散、彗差、球差等
对噪声相对稳健
适合光学像差分析和 DM 标定
```

后续项目中推荐优先考虑 Zernike 拟合。

---

## 20. DM 坐标与哈特曼坐标映射

即使能够重建波前，也需要建立哈特曼测量坐标与 DM 坐标之间的映射关系。

需要考虑：

```text
倍率
平移
旋转
镜像翻转
可能的畸变
```

可用仿射模型近似：

```text
x_DM = a x_HS + b y_HS + c
y_DM = d x_HS + e y_HS + f
```

如果该映射不准确，则无法准确判断测得相位对应 DM 上的哪个区域。

---

## 21. 推荐项目文件组织

建议将 ChatGPT 项目和 Codex / Git 仓库配合管理：

```text
ChatGPT Project: 哈特曼传感器
│
├── 聊天记录：光路、传感器尺寸、DM 尺寸、程序思路
├── 项目说明：系统目标、硬件参数、测量流程
├── 参考文件：光路图、相机参数、DM 参数、MLA 参数
│
└── Codex / Git 仓库
    ├── README.md
    ├── notes.md
    ├── config.yaml
    ├── docs/
    │   ├── optical_layout.md
    │   ├── hartmann_algorithm.md
    │   └── calibration_plan.md
    └── src/
```

ChatGPT 主要用于：

```text
实验逻辑
算法设计
参数讨论
文档整理
```

Codex 主要用于：

```text
代码实现
代码重构
调试
测试
```

---

## 22. 当前阶段的重点结论

1. 哈特曼传感器应使用实际参考图像，而不是理论理想点阵作为基准。
2. 每次改变光阑大小或光路状态后，都应重新拍摄参考图。
3. 当前使用大平面镜代替 DM 时，光阑仍然会显著影响点阵范围。
4. 真实 DM 安装后，如果 DM 成为限制孔径，点阵尺寸将主要由 DM 有效口径和 L3/L4 倍率决定。
5. 点阵不能超过传感器范围，边缘应留出足够余量。
6. 当前相机有效尺寸约为 `4.97 mm × 3.73 mm`。
7. 当前 DM 参数对应的程序网格 / 近似有效口径约为 `4.8 mm × 4.8 mm`，最外侧 actuator 中心跨度约为 `4.4 mm × 4.4 mm`。
8. 程序核心流程应为：
   ```text
   reference image
   → measurement image
   → spot displacement
   → local slope
   → wavefront reconstruction
   → phase map
   ```
9. 第一版程序应优先实现稳定的光斑识别和位移矢量计算。
10. 后续再实现 Zernike 拟合、DM 响应矩阵和闭环校正。

---

## 23. 下一步建议

近期建议优先完成以下工作：

```text
1. 固定当前光阑大小，获得稳定清晰的哈特曼点阵；
2. 拍摄参考图，并开发参考光斑提取程序；
3. 人工检查并保存 reference_points；
4. 拍摄测试图，实现局部窗口内光斑跟踪；
5. 输出 dx / dy 位移图和矢量图；
6. 在 DM 位置放置模拟孔径，验证未来 DM 有效口径对应的哈特曼点阵范围；
7. 根据传感器尺寸和 DM 尺寸，确认 L3/L4 倍率是否合适；
8. 在光斑跟踪稳定后，实现波前斜率计算；
9. 最后再实现相位重建和 Zernike 分析。
```
